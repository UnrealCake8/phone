import { useState } from 'react'; import { browserSupabase } from '@/lib/supabase';
export function AuthForm(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [mode,setMode]=useState<'login'|'register'|'reset'>('login');const [message,setMessage]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();const supabase=browserSupabase();if(mode==='reset'){const {error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${location.origin}/auth`});setMessage(error?.message??'Check your inbox for a reset link.');return}const result=mode==='login'?await supabase.auth.signInWithPassword({email,password}):await supabase.auth.signUp({email,password});setMessage(result.error?.message??(mode==='register'?'Check your email to confirm your account.':'Signed in. Open the app.'))};return <form className="card" onSubmit={submit}><h1>{mode==='login'?'Welcome back':mode==='register'?'Create account':'Reset password'}</h1><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label>{mode!=='reset'&&<label>Password<input type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)}/></label>}<button className="button">{mode==='login'?'Sign in':mode==='register'?'Register':'Send reset email'}</button><button type="button" className="text-button" onClick={async()=>{const {error}=await browserSupabase().auth.signInWithOAuth({provider:'google',options:{redirectTo:`${location.origin}/app`}});if(error)setMessage(error.message)}}>Continue with Google</button><p>{message}</p><button type="button" className="text-button" onClick={()=>setMode(mode==='login'?'register':mode==='register'?'reset':'login')}>{mode==='login'?'Need an account? Register':mode==='register'?'Forgot password?':'Back to sign in'}</button></form>}
